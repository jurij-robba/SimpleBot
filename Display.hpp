#ifndef DISPLAY_HPP
#define DISPLAY_HPP

#include "Model.hpp"

// static class used to display data
class Display {
public:

    // prints information from the model
    static void Print(const Model& model);
};

#endif
